local _ = require("gettext")
return {
    fullname = _("Incremental Reading"),
    description = _([[Review highlighted passages with spaced repetition. Export highlights to a review queue and grade them with an SM-20 algorithm.]]),
}
